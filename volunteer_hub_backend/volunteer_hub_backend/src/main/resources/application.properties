spring.application.name=volunteer_hub_backend
spring.profiles.active=dev
# spring.profiles.active=prod

# Server port
server.port=${PORT:8080}

# JWT Configuration
# Secret key phải có ít nhất 32 bytes (256 bits) cho HS256
# Trong production, sử dụng biến môi trường JWT_SECRET
jwt.secret=${JWT_SECRET:ibRqDXOInzA6F95rQKAbCp5Y5V7q3XmMsecret12}
# Token expiration: 24 giờ = 86400000 ms
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# # Logging (chung)
# logging.level.vn.techmaster.nowj.security=DEBUG
# logging.level.vn.techmaster.nowj.controller=DEBUG

# Google OAuth (chung - nên thay secret khi production)
# Sử dụng giá trị mặc định để tránh lỗi khi thiếu env vars (OAuth sẽ không hoạt động với giá trị mặc định)
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:disabled}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:disabled}
spring.security.oauth2.client.registration.google.scope=profile,email

# Actuator (ignore warning if using Spring Boot 3+)
# management.endpoints.web.exposure.include=health,info
# management.endpoint.health.show-details=always
spring.jpa.open-in-view=false
# Hibernate 6.x auto-detects PostgreSQL version
# Email
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=thaiviethoang@gmail.com
spring.mail.password=nrgy pkui jajj njjx
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Redis
spring.data.redis.port=6379
spring.data.redis.repositories.enabled=false

# Cache TTL for dashboard top posts (seconds). Default 60s.
dashboard.top-posts.cache-ttl-seconds=60

# Password Recovery Settings
recovery.token.ttl-minutes=15
rate-limit.forgot-password.max-attempts=3
rate-limit.forgot-password.window-minutes=60

# Frontend URL (for email links)
app.frontend.url=http://localhost:3000

# File Upload Configuration
# Maximum file size: 10MB (10485760 bytes)
spring.servlet.multipart.max-file-size=10MB
# Maximum request size: 10MB
spring.servlet.multipart.max-request-size=10MB

# OAuth2 callback path (frontend will receive JWT token here)
app.oauth2.redirect-path=/oauth/callback

push.vapid.public-key=BMX5Q9amGFYvmzLVRgUXuQh4n32H1r7L6hcTXWB1-RNKwupiSkAnaD-xqzMXg5gIHD5j4HSF6E71_Iz6c7bC3Js
push.vapid.private-key=VJH4PiPSibWcyX_Z59hVSLA0Nb6IOzv67ZbrTpu21O8
push.vapid.subject=mailto:thaiviethoang2910@gmail.com